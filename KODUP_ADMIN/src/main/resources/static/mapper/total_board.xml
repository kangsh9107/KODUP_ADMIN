<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kodup.admin.totalboard.AdminTotalboardMapper">

   <select id="totList" parameterType="com.kodup.admin.totalboard.PageVo" resultType="Integer">
		select count(sno) totSize from board
		where subject   like '%${findStr}%'
	</select>
     <select id="select" parameterType="com.kodup.admin.totalboard.PageVo" resultType="com.kodup.admin.totalboard.AdminTotalboardVo">
        select b.sno, b.boardtype, b,subject, m.nickname, date_format(b.nal, '%y-%m-%d') b.nal, b.viewcount
        from board b join member m on m.id = b.id
        WHERE id=#{findStr} OR subject LIKE '%${findStr}%'
        order by sno desc
       
	</select>
</mapper>